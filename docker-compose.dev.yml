version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    depends_on:
      - postgres
    ports:
    - "8000:8000"

  postgres:
    image: postgres:15-alpine
    volumes:
      - postgresdata:/var/lib/postgresql/data/file-manager
    ports:
      - "5432:5432"

volumes:
  postgresdata:
